# URL publique de l'API backend (OBLIGATOIRE au build — injectée dans le bundle)
# Dev local :
# VITE_API_URL=http://localhost:4000
# Prod (front 10banc.com + API api.10banc.com) :
VITE_API_URL=https://api.10banc.com

# Debug export / clips (optionnel)
# VITE_CLIP_DEBUG=false
